# devops-netology

"Hello world"

# Из за созданного файла .gitignore в каталоге terraform не попадут в коммит лишние файлы которые не должны попасть в репозиторий

# Новая строка для домашки 3.3"new line" 
